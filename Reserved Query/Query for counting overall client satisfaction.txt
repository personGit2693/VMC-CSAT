SELECT scores_tab.score_name AS 'Rating', 
COUNT(questionresponses_tab.questionresponse_id) AS 'Responses'
FROM questionresponses_tab 
RIGHT JOIN scores_tab 
ON questionresponses_tab.score_id = scores_tab.score_id 
GROUP BY scores_tab.score_name;